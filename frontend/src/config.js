// Alaap Frontend Configuration
// By routing through Vite's proxy, we keep connections relative to the active hostname
export const API_BASE_URL = '/api';
export const SOCKET_URL = window.location.origin;
